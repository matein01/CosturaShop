@model List<CosturaShop.Models.Combo>

<h1>Catálogo de Combos</h1>

<a asp-action = "Create" class="btn btn-success btn-sm">Agregar</a>

<table class="table table-striped">
  <thead>
      <tr>
          <th>Nombre</th>
          <th>Precio</th>
          <th>Chaqueta</th>
          <th>Pantalon</th>
          <th>Editar</th>
          <th>Eliminar</th>
      </tr>
  </thead>
  <tbody>
    @foreach (var combo in Model)
    {
      <tr>
          <td>@combo.Nombre</td>
          <td>@combo.Precio</td>
          <td>@combo.Chaqueta?.Nombre</td>
          <td>@combo.Pantalon?.Nombre</td>
          <td><a asp-action="Edit" asp-route-idComboEditar="@combo.Id" class="btn btn-warning  btn-sm">Editar</a></td>
          <td><a asp-action="Delete" asp-route-idComboEliminar="@combo.Id" class="btn btn-danger btn-sm">Eliminar</a></td>
      </tr>
    }
  </tbody>
</table>