@model List<CosturaShop.Models.Producto>

<h1>Catálogo de Productos</h1>

<a asp-action = "Create">Agregar</a>

<table class="table table-striped">
  <thead>
      <tr>
          <th>Nombre</th>
          <th>Precio</th>
          <th>Tipo</th>
          <th>Editar</th>
          <th>Eliminar</th>
      </tr>
  </thead>
  <tbody>
    @foreach (var producto in Model)
    {
      <tr>
          <td>@producto.Nombre</td>
          <td>@producto.Precio</td>
          <td>@producto.Tipo</td>
          <td><a asp-action="Edit" asp-route-idProductoEditar="@producto.Id">Editar</a></td>
          <td><a asp-action="Delete" asp-route-idProductoEliminar="@producto.Id">Eliminar</a></td>
      </tr>
    }
  </tbody>
</table>